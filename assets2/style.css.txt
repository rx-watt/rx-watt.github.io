/* Reset some default styles */
body, h1, h2, p {
    margin: 0;
    padding: 0;
}

body {
    font-family: 'Nunito', sans-serif;
    line-height: 1.6;
    color: #495961;
    background-color: #232323; /* Darker theme */
}

/* Navigation Styles */
nav {
    background-color: #000000; /* Black background for the navigation bar */
    padding: 10px;
    display: flex;
    align-items: center;
    justify-content: flex-start; /* Align titles to the left */
}

nav a {
    color: #ffffff;
    text-decoration: none;
    font-weight: bold;
    margin-right: 20px; /* Add margin between navigation titles */
}

/* Logo Styles */
.logo-container {
    margin-left: 10px;
    max-height: 80px; /* Adjust the maximum height as needed */
    display: flex;
    align-items: center; /* Center the logo vertically */
}

.logo-container img {
    max-height: 80px; /* Adjust the maximum height as needed */
    height: auto; /* Maintain aspect ratio while resizing */
    width: auto; /* Ensure the image doesn't exceed the container width */
}

/* Two-column Section Styles */
.two-column-section {
    display: flex;
    align-items: stretch; /* Make the columns stretch to the full height */
    overflow: hidden; /* Ensure the background image doesn't overflow */
    margin-top: -40px; /* Adjust the margin to make the two sections merge smoothly */
}

.two-column-section .content {
    flex: 1;
    padding: 50px;
    text-align: left;
    z-index: 1;
    position: relative;
}

.two-column-section .content::before {
    content: "";
    background: url('../images/rx_back2.jpg') left top/50% 100% no-repeat fixed;
    position: absolute;
    top: 0;
    left: 0;
    width: 50%;
    height: 100%;
    z-index: -1;
    background-size: cover;
}

/* Homepage Styles */
header {
    position: relative;
    height: 65vh; /* 30% longer than 50vh */
    display: flex;
    align-items: center;
    justify-content: flex-start; /* Align to the left */
    color: #ffffff;
    text-align: left; /* Align text to the left */
    overflow: hidden;
    cursor: pointer;
}

header::before {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 130%; /* 30% longer than 100% */
    background: url('../images/rx_background.jpg') center/cover no-repeat fixed;
    z-index: -1;
}

header::after {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    background: #258F95; /* 30% opacity black overlay */
    opacity: 0.3;
    z-index: -1;
}

header h1 {
    font-size: 3em;
    margin-bottom: 20px;
    opacity: 0; /* Start with zero opacity */
    animation: fadeIn 1s ease-in-out 1s forwards; /* Animation for fade in */
}

/* Section Styles */
section {
    padding: 80px 0;
    text-align: center;
    color: #ffffff;
    display: flex;
    justify-content: space-between;
    align-items: center;
    flex-direction: column; /* Set the main axis to column */
}

section:nth-child(even) {
    background-color: #495961; /* Grey */
}

section:nth-child(odd) {
    background-color: #258F95; /* Blue */
}

section h2 {
    margin-bottom: 30px;
}

/* Contact Section Styles */
#contact {
    background-color: #258F95; /* Blue */
}

/* Team Section Styles */
#team {
    background-color: #495961; /* Grey */
}

/* Footer Styles */
footer {
    background-color: #232323; /* Dark background */
    color: #ffffff;
    padding: 20px 0;
    text-align: center;
}

/* Placeholder Page Styles */
.placeholder {
    text-align: center;
    padding: 50px;
    background-color: #232323; /* Dark background */
}

/* General Text Styles */
p {
    font-size: 1.1em;
    margin-bottom: 20px;
}

/* Navigation Hover Effect */
nav a:hover {
    border-bottom: 2px solid #ffffff;
}

/* Responsive Styles */
@media (max-width: 768px) {
    nav {
        flex-direction: column;
        align-items: flex-start;
    }

    nav a {
        display: block;
        margin: 10px 0;
    }

    header h1 {
        font-size: 2.5em;
    }

    section {
        padding: 50px 0;
        flex-direction: column;
        text-align: left;
    }

    .two-column-section {
        flex-direction: column;
    }

    .two-column-section .content::before {
        content: "";
        background: url('../images/rx_background2.jpg') left top/50% 100% no-repeat fixed;
        position: absolute;
        top: 0;
        left: 0;
        width: 100%;
        height: 100%;
        z-index: -1;
        background-size: cover;
    }

    footer {
        padding: 15px 0;
    }
}

/* Button Styles */
button {
    background-color: #258F95; /* Blue */
    color: #ffffff;
    border: none;
    padding: 10px 20px;
    font-size: 1em;
    cursor: pointer;
    transition: background-color 0.3s;
}

button:hover {
    background-color: #157478;
}

/* Form Styles */
form {
    max-width: 600px;
    margin: 0 auto;
}

input, textarea {
    width: 100%;
    padding: 10px;
    margin-bottom: 15px;
    border: 1px solid #ccc;
    border-radius: 5px;
}

/* Slideshow Styles */
.slideshow-container {
    max-width: 1000px;
    position: relative;
    margin: auto;
}

.slide {
    display: none;
}

img.slide-image {
    width: 100%;
    height: auto;
}

/* Animation for fade in */
@keyframes fadeIn {
    to {
        opacity: 1;
    }
}

/* Additional Styles */
/* Add any other specific styles you may need in this section */
